### ADR-004: Use Flyway for Database Migrations

- **Status**: Accepted
- **Context**: The database schema needs to be version-controlled and reproducible across environments.
- **Decision**: Use Flyway for database migrations, with migration scripts in `classpath:db/migration`.
- **Consequences**: Schema changes are version-controlled and applied automatically on startup. Ensures consistency across environments.