### ADR-003: Use Nginx as Reverse Proxy

- **Status**: Accepted
- **Context**: The frontend and backend need to communicate through a single entry point.
- **Decision**: Use Nginx to serve the React SPA and reverse-proxy `/api/` requests to the backend.
- **Consequences**: Single entry point, CORS handling, static file serving. The frontend and backend are decoupled.