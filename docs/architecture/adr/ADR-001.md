### ADR-001: Use JWT for Authentication

- **Status**: Accepted
- **Context**: The application needs stateless authentication for REST API endpoints.
- **Decision**: Use JWT (JSON Web Tokens) with Spring Security for authentication. No server-side sessions.
- **Consequences**: Stateless authentication simplifies scaling. Tokens are stored in the browser's localStorage. No session management overhead.